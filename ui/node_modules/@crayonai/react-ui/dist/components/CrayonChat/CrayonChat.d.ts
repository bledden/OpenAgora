import { CreateMessage, Message, processStreamedMessage, ResponseTemplate, Thread, ThreadListManager, ThreadManager } from "@crayonai/react-core";
import { ScrollVariant } from "../../hooks/useScrollToBottom";
import { ThemeProps } from "../ThemeProvider";
type CrayonChatProps = {
    processMessage?: (params: {
        threadId: string;
        messages: Message[];
        abortController: AbortController;
    }) => Promise<Response>;
    onUpdateMessage?: (props: {
        message: Message;
    }) => void;
    processStreamedMessage?: typeof processStreamedMessage;
    responseTemplates?: ResponseTemplate[];
    theme?: ThemeProps;
    createThread?: (message: CreateMessage) => Promise<Thread>;
    threadManager?: ThreadManager;
    threadListManager?: ThreadListManager;
    logoUrl?: string;
    agentName?: string;
    type?: "copilot" | "standalone";
    scrollVariant?: ScrollVariant;
    messageLoadingComponent?: () => React.ReactNode;
    disableThemeProvider?: boolean;
};
export declare const CrayonChat: ({ processMessage, threadManager: userThreadManager, threadListManager: userThreadListManager, logoUrl, agentName, responseTemplates, createThread, onUpdateMessage, processStreamedMessage: userProcessStreamedMessage, messageLoadingComponent, type, theme, scrollVariant, disableThemeProvider, }: CrayonChatProps) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=CrayonChat.d.ts.map