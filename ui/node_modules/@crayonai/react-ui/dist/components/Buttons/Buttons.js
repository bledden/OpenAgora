import { jsx as _jsx } from "react/jsx-runtime";
import clsx from "clsx";
import { forwardRef } from "react";
const variantMap = {
    vertical: "crayon-buttons-vertical",
    horizontal: "crayon-buttons-horizontal",
};
export const Buttons = forwardRef((props, ref) => {
    const { className, style, variant = "horizontal", children, ...rest } = props;
    return (_jsx("div", { ref: ref, className: clsx("crayon-buttons", variantMap[variant], className), style: style, ...rest, children: children }));
});
Buttons.displayName = "Buttons";
//# sourceMappingURL=Buttons.js.map