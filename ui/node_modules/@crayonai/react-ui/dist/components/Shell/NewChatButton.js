import { jsx as _jsx } from "react/jsx-runtime";
import { useThreadListActions } from "@crayonai/react-core";
import clsx from "clsx";
import { Plus } from "lucide-react";
import { Button } from "../Button";
export const NewChatButton = ({ className }) => {
    const { switchToNewThread } = useThreadListActions();
    return (_jsx(Button, { className: clsx("crayon-shell-new-chat-button", className), iconRight: _jsx(Plus, {}), onClick: switchToNewThread, children: "New Chat" }));
};
//# sourceMappingURL=NewChatButton.js.map