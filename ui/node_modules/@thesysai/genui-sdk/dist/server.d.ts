export declare const makeC1Response: () => {
    responseStream: ReadableStream<string>;
    writeThinkItem: ({ title, description: content, ephemeral, }: {
        title: string;
        description: string;
        ephemeral?: boolean;
    }) => Promise<void>;
    writeContent: (content: string) => Promise<void>;
    /**
     * Return custom markdown as the response.
     * @param content - The markdown content to write.
     */
    writeCustomMarkdown: (content: string) => Promise<void>;
    end: () => Promise<void>;
    getAssistantMessage: () => {
        role: string;
        content: string;
    };
};

export { }


declare module '@tanstack/react-table' {
    interface TableMeta<TData extends RowData> {
        updateData: (rowIndex: number, columnId: string, value: unknown) => void;
    }
    interface ColumnMeta<TData extends RowData, TValue> {
        type?: CellType;
        options?: Array<{
            value: string;
            label: string;
        }>;
    }
}
