[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -e .",
  "cd ui && npm install"
]

[phases.build]
cmds = [
  "cd ui && npm run build"
]

[start]
cmd = "uvicorn bazaar.api:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PYTHONPATH = "/app/src"
